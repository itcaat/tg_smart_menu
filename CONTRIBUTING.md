# Руководство по добавлению контента

Это руководство поможет вам добавлять и редактировать материалы в карте канала.

## 📝 Структура данных

Все данные хранятся в файле `data/map.json`. Он состоит из трех основных частей:

1. **meta** - метаинформация о карте
2. **sections** - список разделов
3. **items** - список материалов

## 🆕 Добавление нового раздела

1. Откройте файл `data/map.json`
2. Найдите массив `sections`
3. Добавьте новый объект:

```json
{
  "id": "уникальный-id",
  "title": "Название раздела",
  "description": "Краткое описание раздела",
  "order": 60
}
```

### Правила для разделов:

- **id** - уникальный идентификатор (латиница, цифры, дефисы)
- **title** - название на любом языке
- **description** - краткое описание (1-2 предложения)
- **order** - число для сортировки (меньше = выше в списке)

### Пример:

```json
{
  "id": "ai-ml",
  "title": "AI и Machine Learning",
  "description": "Искусственный интеллект, нейросети, LLM",
  "order": 60
}
```

## 📌 Добавление нового материала

1. Откройте файл `data/map.json`
2. Найдите массив `items`
3. Добавьте новый объект:

```json
{
  "id": "уникальный-id",
  "sectionId": "id-существующего-раздела",
  "title": "Название материала",
  "description": "Описание материала",
  "tags": ["тег1", "тег2", "тег3"],
  "url": "https://t.me/channel/123",
  "pinned": false
}
```

### Правила для материалов:

- **id** - уникальный идентификатор
- **sectionId** - должен совпадать с `id` одного из разделов
- **title** - название материала
- **description** - краткое описание (2-3 предложения)
- **tags** - массив тегов (3-5 тегов оптимально)
- **url** - полная ссылка на материал
- **pinned** - (необязательно) `true` для закрепления в начале списка

### Пример:

```json
{
  "id": "gpt4-review",
  "sectionId": "ai-ml",
  "title": "Обзор GPT-4: что нового?",
  "description": "Подробный разбор возможностей GPT-4, сравнение с предыдущими версиями и примеры использования",
  "tags": ["gpt4", "openai", "llm"],
  "url": "https://t.me/example_channel/500",
  "pinned": true
}
```

## 🏷️ Рекомендации по тегам

Хорошие теги помогают пользователям находить материалы через поиск:

✅ **Хорошие теги:**
- `react`, `typescript`, `kubernetes`
- `api`, `rest`, `graphql`
- `security`, `authentication`, `oauth`

❌ **Плохие теги:**
- Слишком общие: `программирование`, `разработка`
- Дублируют название: если материал называется "React Hooks", не добавляйте тег `react-hooks`
- Очень длинные: `полное-руководство-по-настройке`

### Советы:
- Используйте 3-5 тегов на материал
- Теги на английском лучше для технического контента
- Используйте kebab-case для составных тегов: `machine-learning`, `ci-cd`

## 📋 Чеклист перед добавлением

Перед сохранением изменений убедитесь:

- [ ] Все `id` уникальны
- [ ] `sectionId` в материалах соответствуют существующим разделам
- [ ] Все URL начинаются с `http://` или `https://`
- [ ] JSON файл валиден (нет лишних запятых, скобок)
- [ ] Описания информативные и краткие
- [ ] Теги релевантны и не дублируют название

## ✅ Проверка валидности

После внесения изменений проверьте файл:

```bash
# Проверка JSON синтаксиса
npm run validate

# Или вручную
node scripts/validate-map.js
```

Этот скрипт проверит:
- Валидность JSON
- Уникальность ID
- Наличие обязательных полей
- Соответствие `sectionId` существующим разделам
- Корректность URL

## 🔄 Процесс добавления контента

### Локально:

1. Откройте `data/map.json`
2. Внесите изменения
3. Запустите `npm run validate`
4. Запустите `npm run dev` для проверки
5. Откройте http://localhost:3000 и проверьте отображение
6. Сохраните изменения

### Через GitHub:

1. Создайте новую ветку
2. Внесите изменения в `data/map.json`
3. Создайте Pull Request
4. После проверки - смержите в main
5. Изменения автоматически задеплоятся

## 📊 Организация материалов

### Принципы структурирования:

1. **Разделы** - широкие темы (DevOps, Frontend, Backend)
2. **Материалы** - конкретные посты, статьи, подборки
3. **Теги** - ключевые слова для поиска

### Когда создавать новый раздел:

✅ Создавайте, если:
- Тема значительно отличается от существующих
- Планируется 5+ материалов по теме
- Тема интересна целевой аудитории

❌ Не создавайте, если:
- Материалов мало (1-2), лучше добавить в существующий раздел
- Тема слишком узкая
- Можно объединить с существующим разделом

## 🎨 Стилистика

### Названия разделов:
- Краткие (1-3 слова)
- Понятные широкой аудитории
- Существительные: "DevOps", "Frontend", "Security"

### Названия материалов:
- Информативные
- Включают ключевые слова
- Формат: "Тема: подробность" или "Тема для цели"
- Примеры:
  - ✅ "Kubernetes: сетевые политики"
  - ✅ "Оптимизация React для production"
  - ❌ "Интересная статья"
  - ❌ "Читайте обязательно!"

### Описания:
- 1-3 предложения
- Отвечают на вопрос "О чем материал?"
- Включают ключевые термины
- Без эмодзи и восклицаний

## 🔧 Массовое редактирование

Для добавления большого количества материалов можно:

1. **Использовать JSON editor** (например, VS Code с расширением)
2. **Написать скрипт** для генерации из CSV/Excel
3. **Использовать шаблон**:

```json
{
  "id": "",
  "sectionId": "",
  "title": "",
  "description": "",
  "tags": [],
  "url": "",
  "pinned": false
}
```

Скопируйте шаблон и заполните поля.

## 🐛 Частые ошибки

### 1. Лишняя запятая в конце массива

❌ Неправильно:
```json
{
  "items": [
    {...},
    {...},
  ]
}
```

✅ Правильно:
```json
{
  "items": [
    {...},
    {...}
  ]
}
```

### 2. Несуществующий sectionId

```json
{
  "id": "example",
  "sectionId": "non-existent",  // ❌ Такого раздела нет!
  ...
}
```

### 3. Дублирующиеся ID

```json
{
  "items": [
    { "id": "test", ... },
    { "id": "test", ... }  // ❌ ID должны быть уникальны!
  ]
}
```

## 📞 Помощь

Если возникли вопросы:

1. Проверьте примеры в `data/map.json`
2. Запустите `npm run validate` для проверки
3. Изучите документацию в `README.md`
4. Создайте Issue в репозитории

## 🎯 Примеры хороших материалов

```json
{
  "id": "docker-best-practices",
  "sectionId": "devops",
  "title": "Docker: 10 best practices",
  "description": "Подборка лучших практик для написания Dockerfile: многостадийные сборки, кеширование слоев, безопасность",
  "tags": ["docker", "containers", "best-practices"],
  "url": "https://t.me/example_channel/123",
  "pinned": false
}
```

```json
{
  "id": "react-19-features",
  "sectionId": "frontend",
  "title": "React 19: новые возможности",
  "description": "Обзор React 19: Server Components, Actions, новые хуки и breaking changes",
  "tags": ["react", "react19", "frontend"],
  "url": "https://t.me/example_channel/456",
  "pinned": true
}
```

---

**Приятного редактирования! 🚀**


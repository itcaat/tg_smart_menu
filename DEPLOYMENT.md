# Инструкция по деплою

## 🚀 Быстрый деплой

### GitHub Pages (бесплатно, автоматически)

Самый простой способ - автоматический деплой через GitHub Actions.

**Настройка:**

1. Создайте репозиторий на GitHub
2. Запушьте код в ветку `master`
3. Перейдите в Settings → Pages → Source: GitHub Actions
4. Готово! При каждом push будет автоматический деплой

**URL:**
```
https://<username>.github.io/<repository-name>/
```

**Подробнее:** см. [GITHUB_PAGES.md](./GITHUB_PAGES.md)

### Vercel (рекомендуется для production)

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```
3. Выполните команду в корне проекта:
   ```bash
   vercel
   ```
4. Следуйте инструкциям CLI

Или через веб-интерфейс:
1. Зайдите на [vercel.com/new](https://vercel.com/new)
2. Импортируйте ваш Git репозиторий
3. Vercel автоматически определит Next.js проект
4. Нажмите "Deploy"

### Netlify

1. Создайте аккаунт на [netlify.com](https://netlify.com)
2. Подключите Git репозиторий
3. Настройте команды сборки:
   - Build command: `npm run build`
   - Publish directory: `.next`
4. Нажмите "Deploy site"

### Railway

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий
4. Railway автоматически определит настройки
5. Нажмите "Deploy"

## 📱 Интеграция с Telegram

После деплоя:

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота или используйте существующего:
   ```
   /newbot
   ```
3. Настройте Web App:
   ```
   /newapp
   ```
4. Выберите вашего бота
5. Введите URL вашего задеплоенного приложения
6. Загрузите иконку и введите описание
7. Готово! Теперь можно открыть приложение из меню бота

### Альтернативный способ (через кнопку)

Добавьте кнопку с Web App в сообщения бота:

```python
from telegram import InlineKeyboardButton, InlineKeyboardMarkup, WebAppInfo

keyboard = [
    [InlineKeyboardButton(
        "🗺️ Открыть карту канала",
        web_app=WebAppInfo(url="https://your-app-url.vercel.app")
    )]
]
reply_markup = InlineKeyboardMarkup(keyboard)
```

## 🔧 Переменные окружения

Если в будущем потребуется добавить API ключи или другие секреты, создайте файл `.env.local`:

```env
# Пример переменных
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_CHANNEL_USERNAME=@your_channel
```

⚠️ Переменные с префиксом `NEXT_PUBLIC_` доступны на клиенте.

## 📊 Мониторинг

### Vercel Analytics (бесплатно)

Добавьте аналитику Vercel:

```bash
npm i @vercel/analytics
```

В `app/layout.tsx` добавьте:

```tsx
import { Analytics } from '@vercel/analytics/react';

// В конец body:
<Analytics />
```

## 🔄 Автоматическое обновление контента

Для автоматического обновления `data/map.json`:

1. Настройте GitHub Actions для автоматической сборки при изменении `map.json`
2. Или используйте скрипт для генерации JSON из другого источника (например, Google Sheets, Notion API)

### Пример GitHub Action:

```yaml
name: Deploy on data update
on:
  push:
    paths:
      - 'data/map.json'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Trigger Vercel deployment
        run: curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK }}
```

## 🐛 Отладка

### Проверка билда локально:

```bash
npm run build
npm start
```

### Проверка в production mode:

```bash
npm run build
npx serve@latest out
```

### Логи в Vercel:

1. Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите проект
3. Перейдите в "Deployments" → выберите деплой → "Logs"

## 📱 Тестирование в Telegram

Для тестирования без деплоя используйте ngrok:

```bash
# Установите ngrok
brew install ngrok  # macOS
# или скачайте с https://ngrok.com

# Запустите туннель
ngrok http 3000

# Используйте HTTPS URL от ngrok в Telegram Bot
```

## 🔐 Безопасность

- Все данные статичны и публичны, секретов в коде нет
- Рекомендуется использовать HTTPS (автоматически на Vercel/Netlify)
- Если добавите API - используйте переменные окружения
- Для защиты от спама можно добавить проверку `initData` от Telegram

## 📈 Оптимизация

### Кеширование:

В `next.config.js` можно добавить:

```js
module.exports = {
  // ...existing config
  headers: async () => [
    {
      source: '/data/map.json',
      headers: [
        {
          key: 'Cache-Control',
          value: 'public, max-age=3600, stale-while-revalidate',
        },
      ],
    },
  ],
};
```

### Сжатие изображений:

Если будете добавлять изображения, используйте Next.js Image:

```tsx
import Image from 'next/image';

<Image 
  src="/icon.png" 
  width={50} 
  height={50} 
  alt="Icon"
/>
```

## 🎨 Кастомизация

### Цвета Telegram:

Приложение автоматически подхватывает цветовую схему Telegram. Для кастомизации отредактируйте `app/globals.css`:

```css
:root {
  --tg-theme-bg-color: #ffffff;
  --tg-theme-text-color: #000000;
  /* ... другие переменные */
}
```

### Шрифты:

Добавьте кастомные шрифты в `app/layout.tsx`:

```tsx
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin', 'cyrillic'] });

// В body:
<body className={inter.className}>
```


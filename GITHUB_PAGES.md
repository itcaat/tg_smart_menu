# 🚀 Деплой на GitHub Pages

Пошаговая инструкция для автоматического деплоя на GitHub Pages.

## 📋 Предварительные требования

- Репозиторий на GitHub
- Ветка `master` (или `main`)
- Файл `.github/workflows/deploy.yml` (уже создан)

## 🎯 Быстрый старт

### 1. Настройка репозитория

Перейдите в настройки репозитория на GitHub:

```
Settings → Pages → Build and deployment
```

Выберите:
- **Source**: GitHub Actions

Это всё! GitHub Actions сам создаст environment для Pages.

### 2. Пуш в master

```bash
git add .
git commit -m "feat: add GitHub Pages deployment"
git push origin master
```

### 3. Ожидание деплоя

GitHub Actions автоматически:
1. ✅ Установит зависимости
2. ✅ Проверит валидность данных (`npm run validate`)
3. ✅ Соберет приложение (`npm run build`)
4. ✅ Задеплоит на Pages

Процесс занимает ~2-3 минуты.

### 4. Проверка

Приложение будет доступно по адресу:

```
https://<username>.github.io/<repository-name>/
```

Например:
```
https://john.github.io/tg_smart_menu/
```

Или если репозиторий называется `<username>.github.io`:
```
https://<username>.github.io/
```

## 🔧 Как это работает

### Workflow файл

`.github/workflows/deploy.yml` запускается:
- При каждом push в ветку `master`
- Вручную через GitHub Actions UI (workflow_dispatch)

### Два джоба

**1. Build** - сборка приложения:
- Установка Node.js 20
- Установка зависимостей через `npm ci`
- Валидация `data/map.json`
- Сборка Next.js в статические файлы
- Загрузка артефакта в GitHub

**2. Deploy** - деплой:
- Публикация артефакта на GitHub Pages

### Особенности для Next.js

Next.js требует специальную конфигурацию для статического экспорта:

```javascript
// next.config.js
output: 'export',          // Статический экспорт
images: { unoptimized: true }, // Без оптимизации изображений
basePath: '...',           // Базовый путь (автоопределяется)
trailingSlash: true,       // Слеши в конце URL
```

### basePath

Workflow автоматически определяет `basePath`:
- Если репозиторий `username.github.io` → basePath пустой
- Если `username/repo-name` → basePath = `/repo-name`

Это позволяет приложению работать корректно в подпапке.

## 📁 Структура артефакта

После сборки создается папка `./out/` со статическими файлами:

```
out/
  ├── _next/          # Next.js ресурсы
  ├── index.html      # Главная страница
  ├── section/
  │   └── [id]/
  │       └── index.html
  ├── favorites/
  │   └── index.html
  ├── recent/
  │   └── index.html
  └── .nojekyll       # Отключает Jekyll на GitHub Pages
```

Файл `.nojekyll` важен - он говорит GitHub Pages не использовать Jekyll.

## 🐛 Отладка

### Проверка workflow

Зайдите в:
```
Actions → Deploy to GitHub Pages
```

Выберите последний запуск и посмотрите логи.

### Частые проблемы

**1. 404 при открытии страницы**

Проблема: не настроен basePath или неправильный URL.

Решение: 
- Проверьте Settings → Pages → URL
- Убедитесь что используете правильный URL

**2. Ошибка валидации**

Проблема: невалидный `map.json`.

Решение:
```bash
npm run validate
```

Исправьте ошибки в `data/map.json`.

**3. Ошибка сборки**

Проблема: TypeScript или ESLint ошибки.

Решение:
```bash
npm run build
```

Исправьте ошибки локально перед пушем.

**4. Приложение собралось, но не работает**

Проблема: возможно проблема с basePath или роутингом.

Решение:
- Проверьте браузерную консоль (F12)
- Проверьте что все ссылки относительные

## 🔄 Ручной запуск

Можно запустить деплой вручную:

1. Зайдите в GitHub Actions
2. Выберите "Deploy to GitHub Pages"
3. Нажмите "Run workflow"
4. Выберите ветку и запустите

## 🌍 Custom Domain

Для использования своего домена:

### 1. Настройка DNS

Добавьте CNAME запись:
```
your-domain.com → username.github.io
```

### 2. Настройка Pages

```
Settings → Pages → Custom domain
```

Введите `your-domain.com` и сохраните.

### 3. Обновите next.config.js

Если используете custom domain, уберите basePath:

```javascript
basePath: '', // Пустой для custom domain
```

## 📊 Мониторинг

### Статус деплоя

Badge для README:

```markdown
![Deploy](https://github.com/username/repo/actions/workflows/deploy.yml/badge.svg)
```

### Логи

Все логи доступны в GitHub Actions:
- Время сборки
- Ошибки
- Предупреждения

## 🔐 Безопасность

### Permissions

Workflow использует минимальные права:
- `contents: read` - чтение кода
- `pages: write` - публикация на Pages
- `id-token: write` - токены для деплоя

### Секреты

Не требуются! Всё работает через встроенные GitHub токены.

## ⚡ Оптимизация

### Кеширование

Workflow кеширует `node_modules`:

```yaml
- uses: actions/setup-node@v4
  with:
    cache: 'npm'
```

Это ускоряет повторные сборки.

### Concurrency

Workflow отменяет предыдущие запуски:

```yaml
concurrency:
  group: 'pages'
  cancel-in-progress: true
```

## 📝 Обновление контента

Для обновления контента:

1. Отредактируйте `data/map.json`
2. Проверьте: `npm run validate`
3. Закоммитьте: `git commit -am "update: add new content"`
4. Запушьте: `git push`

GitHub Actions автоматически задеплоит изменения!

## 🔄 Откат версии

Для отката к предыдущей версии:

### Способ 1: Revert commit

```bash
git revert <commit-hash>
git push
```

### Способ 2: Ручной деплой старой версии

1. Перейдите в Actions
2. Найдите успешный старый деплой
3. "Re-run jobs"

## 🎨 Кастомизация workflow

### Добавление тестов

```yaml
- name: Run tests
  run: npm test
```

### Добавление линтера

```yaml
- name: Lint code
  run: npm run lint
```

### Уведомления в Telegram

```yaml
- name: Notify Telegram
  if: success()
  run: |
    curl -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
      -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
      -d text="✅ Deployed to GitHub Pages"
```

## 📚 Полезные ссылки

- [GitHub Pages документация](https://docs.github.com/en/pages)
- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Next.js Static Export](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)

## 🎉 Готово!

После настройки каждый push в `master` будет автоматически деплоить приложение на GitHub Pages.

---

**Нужна помощь?** Создайте Issue в репозитории!


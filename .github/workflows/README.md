# GitHub Actions Workflows

Этот каталог содержит CI/CD workflows для автоматического деплоя.

## 📋 Workflows

### deploy.yml - Деплой на GitHub Pages

Автоматически деплоит приложение на GitHub Pages при каждом push в ветку `master`.

**Триггеры:**
- Push в ветку `master`
- Ручной запуск (workflow_dispatch)

**Шаги:**
1. Checkout кода
2. Установка Node.js 20
3. Установка зависимостей (`npm ci`)
4. Валидация данных (`npm run validate`)
5. Сборка приложения (`npm run build`)
6. Загрузка артефакта в GitHub Pages
7. Деплой на GitHub Pages

**Требования:**
- Настройте GitHub Pages: Settings → Pages → Source: GitHub Actions

**Результат:**
- Приложение доступно по адресу: `https://<username>.github.io/<repository>/`

## 🔧 Кастомизация

### Изменить ветку для деплоя

```yaml
on:
  push:
    branches: ['main']  # Измените на нужную ветку
```

### Добавить тесты

```yaml
- name: Run tests
  run: npm test
```

### Добавить линтер

```yaml
- name: Lint
  run: npm run lint
```

### Деплой только при изменении определенных файлов

```yaml
on:
  push:
    branches: ['master']
    paths:
      - 'app/**'
      - 'components/**'
      - 'data/**'
      - 'lib/**'
```

### Уведомления

Добавьте в конец deploy job:

```yaml
- name: Notify on success
  if: success()
  run: echo "Deploy successful!"

- name: Notify on failure
  if: failure()
  run: echo "Deploy failed!"
```

## 📊 Мониторинг

### Статус деплоя

Badge для README.md:

```markdown
![Deploy Status](https://github.com/<username>/<repo>/actions/workflows/deploy.yml/badge.svg)
```

### Логи

Просмотр логов деплоя:
1. Перейдите в Actions
2. Выберите workflow run
3. Посмотрите детальные логи каждого шага

## 🐛 Отладка

### Workflow не запускается

Проверьте:
- Файл находится в `.github/workflows/`
- YAML синтаксис корректен
- У вас есть права на запуск Actions

### Ошибка валидации

Запустите локально:
```bash
npm run validate
```

Исправьте ошибки в `data/map.json`.

### Ошибка сборки

Запустите локально:
```bash
npm run build
```

Проверьте ошибки TypeScript или Next.js.

### Деплой не отображается

Проверьте:
- Settings → Pages → Source: GitHub Actions
- Workflow завершился успешно
- URL правильный (`https://<username>.github.io/<repo>/`)

## 📚 Полезные ссылки

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [GitHub Pages документация](https://docs.github.com/en/pages)
- [Подробная инструкция](../../GITHUB_PAGES.md)

---

**Нужна помощь?** См. [GITHUB_PAGES.md](../../GITHUB_PAGES.md)


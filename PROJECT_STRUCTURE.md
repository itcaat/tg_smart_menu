# Структура проекта

Полный обзор файлов и директорий приложения "Карта канала".

## 📁 Корневая директория

### Конфигурационные файлы

- **`package.json`** - зависимости и скрипты npm
- **`tsconfig.json`** - настройки TypeScript
- **`next.config.js`** - конфигурация Next.js
- **`tailwind.config.ts`** - настройки Tailwind CSS
- **`postcss.config.js`** - конфигурация PostCSS
- **`.eslintrc.json`** - правила ESLint
- **`.gitignore`** - игнорируемые Git файлы

### Документация

- **`README.md`** - основная документация проекта
- **`DEPLOYMENT.md`** - инструкции по деплою
- **`CONTRIBUTING.md`** - руководство по добавлению контента
- **`PROJECT_STRUCTURE.md`** - этот файл

## 📂 /app - Страницы приложения (Next.js App Router)

### Главные файлы

- **`layout.tsx`** - корневой layout приложения
  - Подключает глобальные стили
  - Добавляет навигацию (TabsNav)
  - Интегрирует TelegramProvider
  - Устанавливает метаданные и viewport

- **`globals.css`** - глобальные стили
  - Tailwind директивы
  - CSS переменные для Telegram тем
  - Кастомные utility классы
  - Responsive стили

- **`TelegramProvider.tsx`** - клиентский компонент для инициализации Telegram WebApp API

### Страницы

- **`page.tsx`** - главная страница (/)
  - Список разделов
  - Глобальный поиск
  - Результаты поиска

- **`/favorites/page.tsx`** - страница избранного (/favorites)
  - Список избранных материалов
  - Работа с localStorage
  - Возможность удаления из избранного

- **`/recent/page.tsx`** - страница недавних (/recent)
  - История просмотров
  - Кнопка очистки истории
  - Последние 30 материалов

- **`/section/[id]/page.tsx`** - страница раздела (/section/:id)
  - Динамический роут
  - Список материалов раздела
  - Поиск внутри раздела
  - Кнопка "Назад"

## 🧩 /components - Компоненты

- **`SectionCard.tsx`** - карточка раздела
  - Название и описание
  - Счетчик элементов
  - Ссылка на раздел

- **`ItemCard.tsx`** - карточка материала
  - Название, описание, теги
  - Кнопки: Открыть, В избранное, Скопировать ссылку
  - Индикатор "закреплено"
  - Интеграция с localStorage

- **`SearchBar.tsx`** - строка поиска
  - Debounce (300ms)
  - Кнопка очистки
  - Иконка поиска

- **`TabsNav.tsx`** - нижняя навигация
  - Три вкладки: Разделы, Избранное, Недавние
  - Индикация активной вкладки
  - Fixed позиция

- **`TagPills.tsx`** - теги (чипсы)
  - Отображение массива тегов
  - Опциональный клик по тегу

## 📚 /lib - Библиотеки и утилиты

- **`data.ts`** - работа с данными
  - Загрузка map.json
  - Получение разделов и материалов
  - Сортировка (по order, pinned, title)
  - Подсчет материалов в разделах

- **`search.ts`** - логика поиска
  - Поиск по title, description, tags
  - Scoring алгоритм
  - Подсветка совпадений (highlightText)

- **`storage.ts`** - работа с localStorage
  - Избранное: добавление, удаление, проверка
  - Недавние: добавление, получение, очистка
  - Ограничение: 30 недавних элементов
  - Версионирование ключей (v1)

- **`telegram.ts`** - интеграция с Telegram WebApp API
  - Определение Telegram окружения
  - Инициализация (ready, expand)
  - Получение цветовой схемы
  - Открытие ссылок

## 🏷️ /types - TypeScript типы

- **`map.ts`** - типы данных
  - `MapMeta` - метаданные
  - `Section` - структура раздела
  - `Item` - структура материала
  - `MapData` - полная структура map.json
  - `SectionWithCount` - раздел со счетчиком
  - `SearchResult` - результат поиска

## 📊 /data - Данные приложения

- **`map.json`** - контент карты канала
  - Метаданные проекта
  - Массив разделов (sections)
  - Массив материалов (items)
  - **Важно**: это единственный источник контента

## 🔧 /scripts - Вспомогательные скрипты

- **`validate-map.js`** - валидация map.json
  - Проверка структуры JSON
  - Валидация обязательных полей
  - Проверка уникальности ID
  - Проверка существования sectionId
  - Вывод статистики

## 📦 Зависимости

### Production

- **next** (^14.2.0) - React фреймворк
- **react** (^18.3.0) - UI библиотека
- **react-dom** (^18.3.0) - React DOM рендерер

### Development

- **typescript** (^5.0.0) - статическая типизация
- **@types/node** - типы для Node.js
- **@types/react** - типы для React
- **@types/react-dom** - типы для React DOM
- **tailwindcss** (^3.4.0) - CSS фреймворк
- **autoprefixer** - CSS автопрефиксер
- **postcss** - CSS обработчик
- **eslint** - линтер
- **eslint-config-next** - ESLint конфиг для Next.js

## 🎯 Основные фичи по файлам

### Маршрутизация

```
/                       → app/page.tsx
/section/[id]          → app/section/[id]/page.tsx
/favorites             → app/favorites/page.tsx
/recent                → app/recent/page.tsx
```

### Источники данных

```
Статические данные      → data/map.json → lib/data.ts
Избранное              → localStorage → lib/storage.ts
Недавние               → localStorage → lib/storage.ts
```

### Стилизация

```
Глобальные стили       → app/globals.css
Tailwind конфиг        → tailwind.config.ts
Компоненты             → inline Tailwind классы
Telegram темы          → CSS переменные + lib/telegram.ts
```

### Поиск

```
Логика поиска          → lib/search.ts
UI поиска (главная)    → app/page.tsx
UI поиска (раздел)     → app/section/[id]/page.tsx
Компонент              → components/SearchBar.tsx
```

## 🔄 Потоки данных

### Загрузка данных

```
data/map.json
    ↓
lib/data.ts (импорт и обработка)
    ↓
Страницы и компоненты (использование)
```

### Избранное

```
Пользователь кликает ⭐
    ↓
components/ItemCard.tsx (handleToggleFavorite)
    ↓
lib/storage.ts (addFavorite / removeFavorite)
    ↓
localStorage (сохранение)
    ↓
app/favorites/page.tsx (отображение)
```

### Недавние

```
Пользователь кликает "Открыть"
    ↓
components/ItemCard.tsx (handleOpen)
    ↓
lib/storage.ts (addRecent)
    ↓
localStorage (сохранение)
    ↓
lib/telegram.ts (openTelegramLink)
    ↓
app/recent/page.tsx (отображение)
```

### Поиск

```
Пользователь вводит текст
    ↓
components/SearchBar.tsx (onChange + debounce)
    ↓
lib/search.ts (searchItems)
    ↓
Страница (отображение результатов)
```

## 🚀 NPM Scripts

```bash
npm run dev        # Запуск dev сервера
npm run build      # Production сборка
npm start          # Запуск production сервера
npm run lint       # ESLint проверка
npm run validate   # Валидация map.json
```

## 🔐 localStorage Ключи

```
favorites:v1       # Массив ID избранных материалов
recent:v1          # Массив ID недавних материалов (макс 30)
```

## 🎨 Tailwind Цветовая схема

### Стандартные цвета

- Светлая тема: белый фон, черный текст
- Темная тема: темно-серый фон, белый текст

### Telegram переменные

```css
--tg-theme-bg-color
--tg-theme-text-color
--tg-theme-hint-color
--tg-theme-link-color
--tg-theme-button-color
--tg-theme-button-text-color
```

Автоматически применяются при открытии в Telegram.

## 📱 Адаптивность

- **Mobile-first** подход
- Все компоненты адаптивны
- Нижняя навигация (TabsNav) fixed на мобильных
- Padding-bottom для контента (pb-20) чтобы не перекрывалась навигация
- Safe area для iOS (viewport-fit=cover)

## 🧪 Тестирование

Для проверки работоспособности:

1. **Валидация данных**: `npm run validate`
2. **Линтер**: `npm run lint`
3. **Сборка**: `npm run build`
4. **Dev сервер**: `npm run dev`

## 📌 Ключевые точки интеграции

### Изменение данных

Редактируйте только `data/map.json`. Все остальное подтянется автоматически.

### Изменение стилей

- Глобальные стили → `app/globals.css`
- Tailwind конфиг → `tailwind.config.ts`
- Компоненты → inline Tailwind классы

### Добавление новых страниц

Создайте `page.tsx` в нужной директории внутри `app/`.

### Добавление новых компонентов

Создайте файл в `components/` и экспортируйте компонент.

---

**Обновлено**: 2025-12-17


# 📖 Инструкция по использованию

Простая автогенерация README.md из `data/map.json`.

## 🚀 Быстрый старт

### 1. Добавление нового материала

Откройте `data/map.json` и добавьте в массив `items`:

```json
{
  "id": "unique-id",
  "sectionId": "devops",
  "title": "Название материала",
  "description": "Краткое описание",
  "tags": ["тег1", "тег2"],
  "url": "https://t.me/channel/123",
  "pinned": false
}
```

### 2. Добавление нового раздела

Добавьте в массив `sections`:

```json
{
  "id": "unique-id",
  "title": "Название раздела",
  "description": "Описание раздела",
  "order": 60
}
```

### 3. Обновление README

**Автоматически:**
```bash
git add data/map.json
git commit -m "add: новый материал"
git push
```

GitHub Actions автоматически обновит README.md! ⚡

**Локально (проверка):**
```bash
npm run generate-readme
```

## 🔍 Проверка данных

Перед коммитом можно проверить валидность:

```bash
npm run validate
```

Скрипт проверит:
- ✅ Уникальность ID
- ✅ Существование разделов
- ✅ Корректность структуры
- ✅ Обязательные поля

## 📊 Структура README

Генерируется автоматически:

```
# Карта канала
> Навигация по материалам

Статистика: разделы • материалы • закрепленные

## 📑 Содержание
- Раздел 1 (count)
- Раздел 2 (count)
...

## Раздел 1
Описание раздела

### 📌 [Закрепленный материал](url)
Описание
**Теги:** `tag1` `tag2`

### [Обычный материал](url)
Описание
**Теги:** `tag1` `tag2`
```

## ✨ Особенности

- **Автосортировка**: по `order` для разделов, по `pinned` и `title` для материалов
- **Закрепление**: материалы с `"pinned": true` показываются первыми с 📌
- **Теги**: отображаются как красивые badges
- **Якоря**: быстрая навигация по разделам через оглавление
- **Статистика**: автоматически считается из данных

## 🎯 GitHub Actions

Workflow `.github/workflows/update-readme.yml`:

**Триггеры:**
- Push в `main` с изменениями `data/map.json`
- Ручной запуск через Actions UI

**Что делает:**
1. Проверяет валидность данных
2. Генерирует новый README.md
3. Коммитит изменения (если есть)

**Важно:** Коммит от бота имеет `[skip ci]` чтобы не создавать бесконечный цикл.

## 📁 Структура файлов

```
data/
  map.json              # Источник данных
scripts/
  validate-map.js       # Валидация структуры
  generate-readme.js    # Генератор README
.github/workflows/
  update-readme.yml     # GitHub Actions
README.md               # Автогенерируемый файл
```

## 💡 Советы

1. **ID должны быть уникальны** в пределах всех items и sections
2. **sectionId должен существовать** в массиве sections
3. **order** определяет порядок разделов (меньше = выше)
4. **pinned: true** поднимает материал в начало раздела
5. **tags** помогают в поиске и группировке

## 🐛 Отладка

### Локальная проверка

```bash
# 1. Валидация
npm run validate

# 2. Генерация
npm run generate-readme

# 3. Просмотр
cat README.md
```

### Проверка GitHub Actions

1. Зайдите в **Actions** на GitHub
2. Найдите "Update README"
3. Посмотрите логи последнего запуска

### Ошибки валидации

Если `npm run validate` показывает ошибки:
- Проверьте уникальность ID
- Проверьте что все sectionId существуют
- Проверьте JSON синтаксис

## 📚 Примеры

### Добавить закрепленный материал

```json
{
  "id": "important-tutorial",
  "sectionId": "devops",
  "title": "⚡ Важный туториал",
  "description": "Обязателен к прочтению",
  "tags": ["tutorial", "важное"],
  "url": "https://t.me/channel/999",
  "pinned": true
}
```

### Создать новый раздел

```json
{
  "id": "databases",
  "title": "Базы данных",
  "description": "PostgreSQL, MongoDB, Redis",
  "order": 45
}
```

## 🔄 Workflow

Типичный процесс работы:

```bash
# 1. Редактируем map.json
vim data/map.json

# 2. Проверяем локально
npm run validate
npm run generate-readme

# 3. Коммитим
git add data/map.json
git commit -m "add: новые материалы по Kubernetes"
git push

# 4. Ждем ~30 секунд
# GitHub Actions обновит README автоматически!
```

## 🎉 Готово!

Теперь вы знаете всё для работы с картой канала!

Просто редактируйте `map.json` и всё остальное произойдет автоматически.

---

**Вопросы?** Создайте Issue в репозитории.

